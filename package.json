{
  "name": "handshake",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint --filter=\"*\"",
    "check-types": "turbo check-types --filter=\"*\"",
    "format": "prettier --write \"**/*.{ts,tsx,json}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,json}\"",
    "test": "turbo test",
    "seed:all": "pnpm --filter @handshake/e2e-seed-pipeline seed:all",
    "seed:fund": "pnpm --filter @handshake/e2e-seed-pipeline seed:fund",
    "seed:hf-fetch": "pnpm --filter @handshake/e2e-seed-pipeline seed:hf-fetch",
    "seed:api": "pnpm --filter @handshake/e2e-seed-pipeline seed:api",
    "seed:onchain": "pnpm --filter @handshake/e2e-seed-pipeline seed:onchain",
    "seed:verify": "pnpm --filter @handshake/e2e-seed-pipeline seed:verify",
    "clean": "turbo clean; find . -name 'dist' -type d -not -path '*/node_modules/*' | xargs rm -rf; find . -name '.turbo' -type d -not -path '*/node_modules/*' | xargs rm -rf"
  },
  "devDependencies": {
    "eslint": "^10.1.0",
    "prettier": "^3.8.1",
    "turbo": "^2.8.21",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  }
}
